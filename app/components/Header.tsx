import Box from "@mui/material/Box";

export default function Header() {
  return (
    <Box
      component="header"
      style={{
        borderBottom: "1px solid #e5e5e5",
        padding: "1rem 1.5rem",
        background: "#fff",
      }}
    ></Box>
  );
}
